name: pricing-snapshot

on:
  schedule:
    - cron: '15 2 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/validate-sources.mjs

  snapshot:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/update-pricing.mjs
      - run: node scripts/generate-index.mjs
      - run: node scripts/generate-readme.mjs
      - name: Commit changes
        run: |
          if [ -n "$(git status --porcelain data docs/index.md README.md)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add data docs/index.md README.md
            git commit -m "chore: refresh intel outputs"
            git push
          else
            echo "No changes"
          fi
